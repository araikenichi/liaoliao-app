# CloneSnap 使用说明

## 🚨 重要提示

**请勿直接双击HTML文件打开！** 必须通过HTTP服务器访问，否则页面无法正常加载。

## ✅ 正确的打开方式

### 方法一：使用快速启动脚本（推荐）

```bash
./run.sh
```

然后在浏览器中访问：`http://localhost:8080/index-leaflet.html`

### 方法二：使用完整启动脚本

```bash
./start.sh
```

根据提示选择：
- 选项 1: Leaflet版（推荐，无需API密钥）
- 选项 2: 百度地图版（需要配置API密钥）

### 方法三：手动启动服务器

使用Python：
```bash
python3 -m http.server 8080
```

然后在浏览器中访问：`http://localhost:8080/index-leaflet.html`

## 📋 版本说明

### Leaflet版（推荐）
- 文件：`index-leaflet.html`
- 地图：使用OpenStreetMap
- 优点：无需配置，开箱即用
- 访问：http://localhost:8080/index-leaflet.html

### 百度地图版
- 文件：`index.html`
- 地图：使用百度地图API
- 需要：配置百度地图API密钥
- 访问：http://localhost:8080/index.html

## ❌ 常见问题

### 问题1：直接双击HTML文件打不开
**原因**：浏览器的CORS安全策略阻止了本地文件访问外部资源。
**解决**：必须通过HTTP服务器访问（使用上述三种方法之一）。

### 问题2：地图显示空白
**原因**：
- 没有通过HTTP服务器访问
- 网络连接问题，无法加载CDN资源
- 浏览器阻止了地理定位权限

**解决**：
1. 确保通过 `http://localhost:8080` 访问
2. 检查网络连接
3. 允许浏览器获取地理位置权限

### 问题3：百度地图版本无法使用
**原因**：未配置百度地图API密钥
**解决**：
1. 使用Leaflet版（推荐）
2. 或者申请百度地图API密钥，参考 `map-config.md`

## 🌟 功能介绍

- **地图页面**：查看附近的用户，显示实时位置
- **聊天页面**：与附近的人聊天
- **相机页面**：拍照和录视频（演示）
- **动态页面**：查看和发布动态内容
- **我的页面**：个人资料和设置

## 💡 提示

1. 首次访问时，浏览器会请求地理位置权限，请点击"允许"
2. 如果拒绝了地理位置权限，应用会使用默认位置（天安门）
3. 所有数据都是模拟数据，用于演示功能

## 🔧 技术要求

- Python 3.x（用于启动HTTP服务器）
- 现代浏览器（Chrome、Firefox、Safari、Edge）
- 网络连接（加载CDN资源）

---

如有问题，请查看 `README.md` 或检查浏览器控制台的错误信息。
